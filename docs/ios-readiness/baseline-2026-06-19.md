# Herafy iOS Readiness Baseline — 2026-06-19

- App: **Herafy**

## Environment

- Repo: `/Users/ziadnasreldin/Documents/GitHub/herafy`
- Node.js: previously verified as `v22.22.3`
- npm: previously verified as `10.9.8`
- Dependencies: `npm ci` completed successfully before this baseline.

## Root app verification

Command group executed:

```bash
npm run lint
npm test
npm run build
npm run e2e
```

Results:

- `npm run lint`: PASS
- `npm test`: PASS
- `npm run build`: PASS
- `npm run e2e`: FAIL

E2E summary:

```txt
2 failed
  [chromium] › tests/e2e/app.spec.ts:9:1 › Arabic RTL landing and search flow work
  [mobile-chrome] › tests/e2e/app.spec.ts:9:1 › Arabic RTL landing and search flow work
1 skipped
9 passed
E2E_EXIT:1
```

Failure:

```txt
locator.click: Error: strict mode violation: getByRole('link', { name: 'عرض الملف' }) resolved to 4 elements
```

Failing file/line:

```txt
tests/e2e/app.spec.ts:47
```

The test currently clicks an ambiguous Arabic "عرض الملف" provider-profile link.

## Firebase Functions verification

Command group executed:

```bash
cd functions
npm run build
npm test
```

Results:

- `npm run build`: FAIL
- `npm test`: PASS

Build failure:

```txt
src/index.ts(1,35): error TS2835: Relative import paths need explicit file extensions in ECMAScript imports when '--moduleResolution' is 'node16' or 'nodenext'. Did you mean './reviews.js'?
src/index.ts(2,35): error TS2835: Relative import paths need explicit file extensions in ECMAScript imports when '--moduleResolution' is 'node16' or 'nodenext'. Did you mean './visibility.js'?
test/reviews.test.ts(2,35): error TS2835: Relative import paths need explicit file extensions in ECMAScript imports when '--moduleResolution' is 'node16' or 'nodenext'. Did you mean '../src/reviews.js'?
test/visibility.test.ts(2,35): error TS2835: Relative import paths need explicit file extensions in ECMAScript imports when '--moduleResolution' is 'node16' or 'nodenext'. Did you mean '../src/visibility.js'?
FUNCTIONS_BUILD_EXIT:2
```

Functions test summary despite build failure:

```txt
Test Files  4 passed (4)
Tests  4 passed (4)
FUNCTIONS_TEST_EXIT:0
```

## iOS/Capacitor status

No iOS wrapper exists yet:

- No `ios/` project verified.
- No `capacitor.config.ts` verified.
- No App Store/TestFlight build exists.

## Next tasks

1. Fix ambiguous Arabic E2E selector.
2. Fix Firebase Functions ESM imports.
3. Add release-check script.
4. Continue tracker from `docs/plans/2026-06-19-ios-app-store-readiness-tracker.md`.
